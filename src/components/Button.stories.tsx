import Button from './Button';
import React from 'react';
export default {
  title: 'Components/Button',
  components: Button,
};

export const Primary = () => (
  <Button
    onClick={() => { }}
  >
    Button
  </Button>
);
